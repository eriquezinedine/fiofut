name: authentication
description: Package de UI global para FioFut. Contiene widgets, estilos de texto, temas de colores, espaciado y extensiones reutilizables.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.9.2
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
