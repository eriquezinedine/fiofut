name: authentication
description: Paquete de autenticacion para FioFut. Maneja Google Sign-In con Supabase y gestion de perfiles.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.9.2
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0
  google_sign_in: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
